apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  type: ClusterIP  # Chỉ mở cổng nội bộ (Bảo mật hơn)
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 8080       # Cổng service mở cho các Pod khác gọi
      targetPort: 8080 # Cổng thật của container